# XMTL 项目代码运行指南

> 项目路径：`C:\Users\Lenovo\Desktop\myproject\LTF_code\`
> 所有命令均在 `LTF_code/` 目录下执行。

---

## 环境要求

### 依赖包（无 requirements.txt，手动安装）

```bash
pip install torch numpy pandas scikit-learn xgboost shap pyyaml matplotlib scipy
```

### Python 版本
项目使用 Python 3.x（推荐 3.9+）。

### 工作目录
**所有命令必须在 `LTF_code/` 目录下运行**，否则相对路径会失效：

```bash
cd C:\Users\Lenovo\Desktop\myproject\LTF_code
```

---

## 关于多版本脚本的说明

`scripts/` 目录下有两个图表脚本：

| 脚本 | 说明 | 优先级 |
|------|------|--------|
| `scripts/generate_figures.py` | 生成完整论文图表（架构图、SHAP图、策略图等） | **优先使用** |
| `scripts/generate_figures_pipeline.py` | 仅生成 Figure 1（策略流程图）和 Figure 2（架构图） | 备选，功能更少 |

其他模块均无多版本，直接使用即可。

---

## 阶段一：数据检查 / 预处理

### 脚本：`src/preprocessing.py`

**作用：** 验证原始数据文件的维度是否符合预期，并生成 train/val/test 划分索引（80/10/10）。

**运行命令：**
```bash
python -m src.preprocessing --config configs/default.yaml
```

**预期输出：**
- 控制台打印各数据文件的 shape 验证结果
- 生成 `data/processed/splits_seed42.npz`（train/val/test 索引）
- 生成 `data/processed/data_verification_report.json`

**输出位置：** `data/processed/`

**可跳过吗？** 如果 `data/processed/splits_seed42.npz` 已存在，可跳过。

---

## 阶段二：Baseline 模型训练

### 脚本：`experiments/run_baselines.py`

**作用：** 训练 5 个 baseline 模型：Ridge（仅内容特征）、Ridge（全特征）、MLP、Random Forest、XGBoost。

**运行命令（完整训练，耗时较长）：**
```bash
python -m experiments.run_baselines --config configs/default.yaml
```

**快速验证（smoke test，约 5000 样本）：**
```bash
python -m experiments.run_baselines --config configs/default.yaml --smoke_test
```

**预期输出：**
- 每个模型的 MAE / RMSE / Spearman / R² 指标
- 各模型的预测结果文件

**输出位置：**
```
outputs/results/baselines/
├── ridge_content_only/   predictions.csv, metrics.json
├── ridge_full/           predictions.csv, metrics.json
├── mlp/                  predictions.csv, metrics.json
├── random_forest/        predictions.csv, metrics.json
└── xgboost/              predictions.csv, metrics.json, model.pkl
```

**可跳过吗？** 如果上述目录已存在且包含 `predictions.csv`，可跳过。

---

## 阶段三：XMTL 模型训练

### 脚本：`training/train.py`

**作用：** 训练主模型 XMTL（多模态时序融合网络），包含早停、学习率调度、checkpoint 保存。

**运行命令（完整训练，最多 100 epochs）：**
```bash
python -m training.train --config configs/default.yaml
```

**仅使用时序特征（消融变体）：**
```bash
python -m training.train --config configs/default.yaml --temporal_only
```

**预期输出：**
- 训练过程中每 epoch 的 train/val loss
- 最终 test 集指标（MAE / RMSE / Spearman / R²）
- 最优模型 checkpoint

**输出位置：**
```
outputs/results/xmtl_full/
├── best_model.pth        最优模型权重
├── predictions.csv       测试集预测结果
├── metrics.json          测试集指标
└── training_history.csv  每 epoch 的 loss 曲线
outputs/checkpoints/      备份 checkpoint
```

**可跳过吗？** 如果 `outputs/results/xmtl_full/best_model.pth` 已存在，可跳过。

---

## 阶段四：评估 & 模型对比

### 脚本：`evaluation/evaluate.py`

**作用：** 扫描所有模型的 `predictions.csv`，重新计算指标，生成跨模型对比表。不需要加载模型权重。

**运行命令（从预测文件评估，推荐）：**
```bash
python -m evaluation.evaluate --config configs/default.yaml
```

**从 XMTL checkpoint 重新推理（可选）：**
```bash
python -m evaluation.evaluate --config configs/default.yaml \
    --checkpoint outputs/results/xmtl_full/best_model.pth
```

**预期输出：**
- 控制台打印所有模型的对比表
- 生成对比 CSV 文件

**输出位置：**
```
outputs/results/
├── comparison.csv              所有模型指标对比
└── final_experiment_table.csv  最终结果表（用于论文）
```

**可跳过吗？** 如果只需要单个模型结果，可跳过。

---

## 阶段五：消融实验

### 脚本：`experiments/ablation.py`

**作用：** 对 XGBoost（快速）和 XMTL（可选）进行消融实验：
- Leave-one-out：每次去掉一个模态，观察性能下降
- Single-modality：每次只保留一个模态，观察单独贡献

**运行命令（仅 XGBoost 消融）：**
```bash
python -m experiments.ablation --config configs/default.yaml
```

**同时包含 XMTL 消融（需要先完成阶段三）：**
```bash
python -m experiments.ablation --config configs/default.yaml \
    --xmtl_checkpoint outputs/results/xmtl_full/best_model.pth
```

**预期输出：**
- 各模态的重要性排名
- Leave-one-out 和 single-modality 结果

**输出位置：**
```
outputs/results/ablation/
├── xgboost_ablation.csv        XGBoost 消融结果
└── xmtl_ablation.csv           XMTL 消融结果（如果提供了 checkpoint）
```

**依赖：** 需要先完成阶段二（Baseline 训练）。

---

## 阶段六：可解释性分析（SHAP）

### 脚本：`explainability/shap_analysis.py`

**作用：** 对 XGBoost 模型运行 SHAP TreeExplainer，计算各模态和各特征的重要性；同时分析 XMTL 的 gated fusion 权重。

**运行命令：**
```bash
python -m explainability.shap_analysis --config configs/default.yaml
```

**预期输出：**
- 各模态 SHAP 重要性（时序特征约占 56%）
- 特征级别的 SHAP 值

**输出位置：**
```
outputs/results/explainability/
├── xgboost_shap.json           模态级 SHAP 重要性
├── shap_values.npy             样本级 SHAP 值矩阵
└── fusion_weight_analysis.json XMTL 融合权重分析
outputs/results/shap_summary.csv
```

**依赖：** 需要先完成阶段二（XGBoost 训练）。

---

## 阶段七：推荐策略生成

### 脚本：`recommendation/strategy_generator.py`

**作用：** 基于 SHAP 重要性和消融结果，为 Bilibili 内容创作者生成 4 种可操作的内容策略建议。

**运行命令：**
```bash
python -m recommendation.strategy_generator --config configs/default.yaml
```

**预期输出：**
- 4 种内容创作策略（JSON 格式）

**输出位置：**
```
outputs/results/recommendations/
└── strategies.json
```

**依赖：** 需要先完成阶段六（SHAP 分析）。

---

## 阶段八：图表生成

### 脚本：`scripts/generate_figures.py`（优先使用）

**作用：** 生成所有论文图表，包括：
- 模型架构图
- SHAP 重要性图
- 消融实验图
- 策略流程图
- 模型对比图

**运行命令：**
```bash
python scripts/generate_figures.py
```

**备选（仅生成 Figure 1 & 2）：**
```bash
python scripts/generate_figures_pipeline.py
```

**预期输出：**
- 多张 PNG / SVG 格式的论文图表

**输出位置：**
```
outputs/figures/
├── architecture.png / .svg
├── shap_importance.png / .svg
├── ablation.png / .svg
├── strategy_pipeline.png / .svg
└── model_comparison.png / .svg
```

**依赖：** 建议在阶段六、七完成后运行，以使用真实数据生成图表。

---

## 阶段九：论文编译（可选）

**作用：** 将 LaTeX 源文件编译为 PDF。

**运行命令：**
```bash
pdflatex paper.tex
bibtex paper
pdflatex paper.tex
pdflatex paper.tex
```

**输出位置：** `paper.pdf`（已存在）

**可跳过吗？** `paper.pdf` 已存在，如未修改 `paper.tex` 可跳过。

---

## 新手执行清单

> 按顺序执行，每步完成后再进行下一步。

```
Step 1:  cd C:\Users\Lenovo\Desktop\myproject\LTF_code

Step 2:  pip install torch numpy pandas scikit-learn xgboost shap pyyaml matplotlib scipy
         （仅首次运行需要）

Step 3:  python -m src.preprocessing --config configs/default.yaml
         验证数据 + 生成 splits_seed42.npz
         → 输出：data/processed/splits_seed42.npz

Step 4:  python -m experiments.run_baselines --config configs/default.yaml
         训练 5 个 baseline 模型（耗时较长，可先用 --smoke_test 验证）
         → 输出：outputs/results/baselines/

Step 5:  python -m training.train --config configs/default.yaml
         训练 XMTL 主模型（最多 100 epochs，有早停）
         → 输出：outputs/results/xmtl_full/best_model.pth

Step 6:  python -m evaluation.evaluate --config configs/default.yaml
         生成跨模型对比表
         → 输出：outputs/results/comparison.csv

Step 7:  python -m experiments.ablation --config configs/default.yaml \
             --xmtl_checkpoint outputs/results/xmtl_full/best_model.pth
         消融实验
         → 输出：outputs/results/ablation/

Step 8:  python -m explainability.shap_analysis --config configs/default.yaml
         SHAP 可解释性分析
         → 输出：outputs/results/explainability/

Step 9:  python -m recommendation.strategy_generator --config configs/default.yaml
         生成内容创作策略
         → 输出：outputs/results/recommendations/strategies.json

Step 10: python scripts/generate_figures.py
         生成所有论文图表
         → 输出：outputs/figures/

Step 11: （可选）pdflatex paper.tex && bibtex paper && pdflatex paper.tex && pdflatex paper.tex
         编译论文 PDF
         → 输出：paper.pdf
```

---

## 依赖关系图

```
Step 3 (预处理)
    └─→ Step 4 (Baseline 训练)
            └─→ Step 6 (评估)
            └─→ Step 7 (消融实验)
            └─→ Step 8 (SHAP 分析)
                    └─→ Step 9 (策略生成)
                            └─→ Step 10 (图表生成)
    └─→ Step 5 (XMTL 训练)
            └─→ Step 6 (评估，可选 checkpoint 模式)
            └─→ Step 7 (消融实验，可选 XMTL 消融)
```

---

## 常见问题

**Q: 训练太慢怎么办？**
- Baseline：加 `--smoke_test` 用 5000 样本快速验证
- XMTL：早停机制会在验证集不再提升 25 个 epoch 后自动停止
- 有 GPU 时会自动使用（`device: auto`）

**Q: 内存不足怎么办？**
- `content_features.npy` 为 3.4GB，需要至少 8GB 内存
- 可在 `configs/default.yaml` 中减小 `batch_size`

**Q: Windows 路径问题？**
- 所有脚本使用相对路径，必须在 `LTF_code/` 目录下运行
- `num_workers: 0` 已为 Windows 兼容性设置好

**Q: 结果已存在，想重新运行某一步？**
- 直接运行对应命令，输出文件会被覆盖
